# Mutation-Style Invariant Verification

Every test that claims to protect an invariant must be **demonstrated to fail when that invariant
is deliberately broken**, and the demonstration must be recorded here.

Established by `P0-TSK-038`. This is not a new practice — it is what every task in Phase 0 has
already been doing — so the task is to make it *repeatable and checkable* rather than to introduce
it.

**Why it is a rule and not a habit.** `DEFINITION_OF_DONE.md` §3 lists, among the things that block
done: *"a test would still pass if the invariant it claims to protect were removed"*.
`PHASE_GATES.md` §3 criterion 3 requires that every in-scope `INV-*` have *"at least one test that
fails if the invariant is broken"*. Neither is satisfiable by inspection: a test that cannot fail
looks exactly like a test that passes, and it is worse than no test, because it is believed. This
phase has now found four of them — `secretsAreWrapped`, two rules in `P0-TSK-041`, and a
clock-skew test named for a property it did not exercise.

---

## 1. What counts as a demonstration

A demonstration has three parts, and all three must be recorded:

1. **The mutation** — the specific change that breaks the invariant, precise enough to re-apply.
2. **The command** — what to run.
3. **The observed result** — which tests failed, by name or by count.

"I checked it" is not a demonstration. Neither is "the test asserts X", which is a statement about
the test rather than about what happens when the property is gone.

### Two admissible forms

| Form | What it is | Strength |
|---|---|---|
| **In-suite** | The proof lives in the suite and runs on every build: a fixture that violates the rule, asserted to be rejected | **Strongest.** Re-run continuously, so it cannot rot |
| **Recorded** | The mutation cannot live in the suite — it drops a constraint, widens a grant, or changes production code — so it is a written procedure with its observed result | Weaker: it is re-runnable but only if someone runs it |

**Prefer in-suite.** Use a recorded procedure only where the mutation is one the suite cannot
contain, and the register says which form each demonstration takes so the difference stays visible.

**A recorded demonstration is not prose.** Prose says "we proved it fails". A recorded
demonstration names the mutation, the command and the result, so a reviewer at the phase gate can
re-run it rather than trust it.

### What the form tells you

An in-suite demonstration proves the rule has teeth *today, on this build*. A recorded one proves
it had teeth *on the day it was written*. That difference is the reason the register exists: at the
Phase 0 exit gate, criterion 3 asks a question about now, and only the first form answers it
without work.

---

## 2. The register — invariants of every phase reached

Every invariant `FINANCIAL_INVARIANTS.md` marks as belonging to a phase this project has reached.
`MutationDemonstrationTest` fails the build if one is missing, or if a row names a test class or
method that does not exist.

**The current phase is derived from `CURRENT_STATE.md` rather than written here** (`P1-TSK-024`), so
Phase 2 needs no change to the guard. It enforced *Phase 0 only* until that task, which left the
`INV-IDN` group in exactly the weaker regime the Phase 0 → 1 transition created it to escape.

**A row may name several tests**, and every one of them is checked. The parser required exactly one
until `P1-TSK-024`, and **nine rows therefore did not parse at all** — eight written during Phase 1 —
so the guard was silently not checking that the tests they name exist.

| Invariant | Demonstrated by | Form | The mutation | Observed |
|---|---|---|---|---|
| `INV-MON-01` | `NoFloatingPointMoneyRulesTest#rulesRejectTheirViolations` | In-suite | Four fixtures, one per rule: a `double` field, a `float` signature, a floating-point call target, a floating-point field access | Rejected on every build. Also proven end to end (`P0-TSK-008`) by a `double` planted in `Money`, a `float` in a signature and a `Double.parseDouble` call, each in a different module |
| `INV-MON-02` | `MoneyColumnsDatabaseTest#schemaRejectsMalformedCurrency` | Recorded | Weaken the currency `CHECK` constraint in the DDL fragment | The constraint tests fail (`P0-TSK-011` review, which added them after probing showed `CHAR(3)` accepts `'US '`) |
| `INV-MON-03` | `MoneyPropertiesTest` | Recorded | Make every `RoundingPolicy` round `CEILING` | The per-policy defining properties fail (`P0-TST-001`) |
| `INV-MON-04` | `MoneyTest` | Recorded | Remove the currency check from `plus`/`minus`/`compareTo` | Cross-currency tests fail (`P0-TST-001` criterion: "currency checking deliberately broken") |
| `INV-MON-05` | `MoneyColumnsDatabaseTest#storedScaleSurvivesIndependentlyOfCurrentCurrencyData` | Recorded | In `MoneyColumns.read`, re-derive the scale from the currency instead of reading the stored column | **Exactly one test fails**, and it is that one. `P0-TSK-038` — see §4 |
| `INV-MON-06` | `MoneyTest` | Recorded | Remove the overflow check from monetary arithmetic | Boundary tests fail (`P0-TST-001` criterion: "overflow handling deliberately broken") |
| `INV-BAL-03` | `AllocationZeroResidualTest` | Recorded | Change the allocator to naive division, discarding the remainder | The sweep fails (`P0-TST-002`) |
| `INV-BAL-02` | `SustainedConcurrentPostingDatabaseTest#replayEqualsTheProjectionThroughoutSustainedPosting`, `SustainedConcurrentPostingDatabaseTest#aRebuildWhilePostingsContinueLosesNothing`, `BalanceProjectionDatabaseTest#aRolledBackPostingLeavesTheProjectionUnchanged`, `ProjectionVerificationDatabaseTest#aCleanAccountVerifiesCleanAndInjectedDriftIsDetected` | Recorded | **The projection updated outside the posting transaction** (ADR-0041 rule 1 violated - the async-projector defect in its smallest form): `JdbcBalanceProjection`'s upserts rerouted onto a private autocommit connection, so each projection update commits independently of the posting that carries it | **Five tests fail across both suites**, the two intended among them: the rolled-back posting no longer takes its projection change with it (deterministic), and the sustained storm's mid-flight sweeps see stable-bracket disagreement - `DRIFTING` where only `CLEAN`/`IN_FLIGHT` is legal. The outside connection also cannot see uncommitted accounts, so the whole write path objects loudly (`P3-TST-001`). The demonstration is the COMPOSITION, sustained: ten instances posting continuously while the verification job runs, replay-from-zero equal to the projection throughout, and a lock-then-look rebuild under live traffic - single pieces were proven by `P3-TSK-009`'s ten-way race and `P3-TSK-010`'s deterministic in-flight interleave, and the `P1-TSK-027` lesson is that two green halves compose only when something drives them together |
| `INV-BAL-04` | `HoldDatabaseTest#aHoldIsBoundedByAvailableBalance`, `HoldDatabaseTest#tenConcurrentPlacementsRespectAvailableBalance`, `HoldDatabaseTest#releaseRestoresAvailabilityExactly`, `HoldDatabaseTest#aCorruptedProjectionChangesNoDecision` | Recorded | Four, from `P3-TSK-015`'s sweep: the availability check dropped; **the decision reading `holds_minor` instead of the hold rows** - the projection as a decision input, which is `INV-BAL-05`'s defect, recorded here with that invariant's own row deferred in writing to the exit review (the `P2-TST-001` handling of `INV-KYC-06`); the release decrement dropped; the converged release acting again | **All caught by the intended assertion.** The boundary test (a hold of exactly available accepted, one minor unit more refused, amount-free) and the ten-way count catch the dropped check. The corrupted-projection test catches the swap: `holds_minor` corrupted through the app role's own narrow grant must change **no** decision - which is what makes `INV-BAL-05` behaviourally catchable here, where `P3-TSK-014` had to record its derivation-vs-projection swap as held by design because the projection is transactional and never behind. The release tests catch both release mutations - the conditional's row count gates the decrement, the record and the event, so availability is restored **exactly** and exactly once |
| `INV-CON-01` | `HoldDatabaseTest#theLosingPlacerIsObservedBlockedThenRefused`, `HoldDatabaseTest#tenConcurrentPlacementsRespectAvailableBalance` | Recorded | Two lock mutations, and they are different defects: the account row's `FOR UPDATE` **removed** (`P3-TSK-015`); the availability check's inputs **moved outside the lock** - derived before the grant, judged after it (performed by `P3-TST-002`, because the check *dropped* is not the check *moved*: the moved form still locks, still checks, and still loses the race - the `P2-TSK-015` write-skew shape wearing hold clothes, and the mutation that passes every sequential test) | **Both caught, by different halves of the same test - which is why it asserts the coordination and not only the outcome.** The removed lock fails the **blocked-observation precondition**: the loser is never seen Lock-waiting in `pg_stat_activity`, and an outcome-only test could not tell that mechanism from the right one. The moved check keeps that precondition green and fails the **outcome half**: the loser blocks, resumes, judges its pre-lock snapshot and wrongly accepts - two holds of 600 in 1000 - and the ten-way race admits more than was available. Intact, ten instances placing 1000 against 3000 accept exactly three, counted in the table, with `holds_minor` equal to the fold of the active rows |
| `INV-LED-01` | `JournalEntryTest#anUnbalancedEntryThrowsWithoutAmounts`, `JournalEntryTest#crossSubsidyBetweenCurrenciesIsRefused`, `JournalEntryPropertiesTest#balancedConstructsAndPerturbedThrows`, `JournalPostingDatabaseTest#aDirectUnbalancedInsertIsRefusedAtCommit`, `TrialBalanceDatabaseTest#injectedImbalancesAreDetectedPerCurrency` | Recorded | Three layers, each mutated by its owning task: the domain balance check removed and the per-currency grouping collapsed onto one bucket (`P3-TSK-004`); V004's balance trigger dropped, and separately its scales clause dropped (`P3-TSK-005`, against raw SQL on a from-scratch database); the trial-balance zero comparison neutralised and its currency buckets collapsed (`P3-TSK-019`) | **All caught by the intended assertion.** The domain sweep runs 2000 generated trials with every one-minor-unit perturbation refused, verified against independent `BigDecimal` arithmetic; the schema half proves every statement succeeds and the COMMIT throws naming the invariant — the deferral is the design, judged when the entry is whole; system-wide, the injected cross-currency subsidy (EUR debit-excess, GBP credit-excess of equal decimal value) is flagged per currency, so a collapsed bucket cannot watch them cancel |
| `INV-LED-02` | `JournalEntryTest#fewerThanTwoLinesIsRefused`, `JournalPostingDatabaseTest#anEntryWithoutLinesIsRefusedAtCommit` | Recorded | The domain line-count refusal removed (`P3-TSK-004`); V004's entry-anchored trigger dropped (`P3-TSK-005`) | Both caught. The load-bearing half is the **empty** entry: a zero-line entry balances vacuously — every currency's two sums are equal at zero — so a balance check alone waves it through, which is why the line count is its own check at the domain and its own trigger at the schema (a line trigger never fires for an entry with no lines) |
| `INV-LED-03` | `JournalPostingDatabaseTest#theColumnSweep`, `JournalPostingDatabaseTest#theAppendOnlyTriggerBindsTheMigrator`, `JournalEntryMigrationTest#theGrantsAreTheHeadline` | Recorded | `UPDATE` granted on the journal tables; separately the append-only trigger dropped (`P3-TSK-005`) | Both caught. The grant sweep probes `UPDATE`/`DELETE` on **every column of both tables** from `information_schema` (the `P0-TST-007` idiom, so a column-level widening cannot hide); the trigger is the second layer and binds the writers the grant does not — the **migrator** edits nothing either, proven directly. Two controls blind in different directions, neither substituting |
| `INV-LED-04` | `LedgerModuleIsolationTest#seesNoSiblingAndNoCompositionRoot`, `AccountsModuleIsolationTest`, `ProjectionVerificationDatabaseTest#theRawSqlWriterIsDetectedAndTheSweepReportsIt` | Recorded | A cross-module dependency planted in both directions, and a `ledger -> accounts` edge planted in the build graph (`P3-TSK-011`); a writer that bypasses `PostingService` entirely — raw SQL inserting journal rows with no projection update (`P3-TSK-010`'s absent-row-made-clean mutation) | The planted dependencies fail the isolation tests, and the reverse edge fails Gradle configuration as a **circular dependency** — the ADR-0042 asymmetry is structural. **The limit is stated rather than glossed**: there is one application role, so a raw-SQL writer holding it CAN insert journal rows — what binds every writer is that V004's triggers refuse unbalanced or line-less entries whoever writes them, and what surfaces a bypass is the verification job, whose absent-projection-row detection was proven by planting exactly those rows. `STATIC` is the wall; the comparison is the net |
| `INV-LED-05` | `PostingServiceDatabaseTest#anUnestablishedActorIsRefused`, `PostingServiceDatabaseTest#onePostingCommitsEverythingTogether` | Recorded | The actor defaulted to the platform when none is established (`P3-TSK-006`) | Caught: `SecurityContext.require()` refuses rather than defaulting, so a posting with no established actor never commits — and the happy path asserts the entry, its audit record (actor, correlation) and its outbox row commit together, the attribution columns `NOT NULL` at `DB-CONSTRAINT` rank in V004 |
| `INV-LED-06` | `LedgerAccountDatabaseTest#theFreezeHasTwoLayersAndBothHold`, `LedgerAccountTest#normalBalanceDerivesFromTheType`, `LedgerAccountMigrationTest#theCoherenceRulesAreTheDerivations` | Recorded | The freeze trigger's posted-probe neutralised; the coherence `CHECK` dropped; the type-to-normal-balance derivation inverted (`P3-TSK-002`; the trigger re-proven against the real `journal_line` by `P3-TSK-005`, as the stand-in's comment promised) | All caught. The demonstration carries its own positive control: the migrator — the role the grant does not bind — corrects an **unposted** account's classification, a journal line lands, and the same coherent update is refused with `check_violation`. So the freeze binds exactly when the invariant says it must, for every writer |
| `INV-BAL-01` | `BalanceDerivationDatabaseTest#replayAgreesWithIndependentArithmeticForEveryReachableType`, `BalanceProjectionTest#thePortDeclaresOnlyVoidWrites`, `BalanceProjectionTest#theVerifierHandsNoBalanceOut` | Recorded | The sign convention inverted; the entry cut made exclusive; the mixed-scale refusal swallowed; `ofPersisted` swapped for `ofMinorUnits` (`P3-TSK-008`) | All caught, each by the intended assertion — the replay is held to **independent `BigDecimal` arithmetic over raw SQL rows**, never certified through `Money`, because the derivation is the definition everything else is checked against. The no-balance-mutation half is **structural and pinned in-suite**: the projection port declares one `void` method and the verifier hands no balance out, so nothing in Java can read the projection into a decision, and a read added in a hurry is a build failure until somebody decides |
| `INV-BAL-03` | `TrialBalanceDatabaseTest#theSweepIsSafeUnderConcurrentPosting`, `OperationalChartDatabaseTest#everyCombinationResolves` | Recorded | The posting half (`Phase: 3`), joining the allocation row above: the designated `ROUNDING_RESIDUAL` seed row removed (`P3-TSK-003`); system-wide conservation is `INV-LED-01`'s triggers plus the trial balance, whose mutations are that row's and `INV-ACC-01`'s | The removed seed fails the hermetic reconciliation **and** the resolve suite — so an allocation with a residual to post always has its designated account (`ChartOfAccounts.resolve` throws on a gap, never an empty `Optional`), and sweeps racing four live posters read zero every time: no committed posting creates or destroys value |
| `INV-BAL-05` | `HoldDatabaseTest#aCorruptedProjectionChangesNoDecision`, `BalanceProjectionTest#thePortDeclaresOnlyVoidWrites` | Recorded | The hold decision reading `holds_minor` instead of the authoritative hold rows (`P3-TSK-015`) — the projection as a decision input, this invariant's defect in its catchable form | Caught: `holds_minor` corrupted through the app role's own narrow grant must change **no** decision, and with the mutation in place it does — which is what made the invariant behaviourally catchable at all, where `P3-TSK-014` recorded its derivation-vs-projection swap as held by design (the projection is transactional and never behind, so the swap is invisible there). The structural half is pinned in-suite: no read exists on the projection port for a decision to consume. Landed by `P3-TST-003`, whose scope demands every `Phase: 3` row — earlier than the deferral `P3-TST-002` recorded, which harms nothing: what the deferral postponed was the record, and this is the record |
| `INV-HIST-01` | `JournalPostingDatabaseTest#theAppendOnlyTriggerBindsTheMigrator`, `JournalPostingDatabaseTest#theColumnSweep`, `ReversalDatabaseTest#aFullReversalLeavesTheOriginalByteIdentical` | Recorded | The append-only trigger dropped and `UPDATE` granted (`P3-TSK-005`); a correction path that edits — the reversal's effect on its original, captured as PostgreSQL's own renderings before and compared equal after (`P3-TSK-016`) | All caught. History is unwritable twice over — the privilege for the application role, the unconditional trigger for everyone including the migrator — and the platform's two correction paths (reversal, adjustment) are both **new entries**: the original is byte-identical after a full reversal, asserted as `e::text` and every line by `seq` |
| `INV-CON-01` | `AccountClosingDatabaseTest#theCloseAndAPostingSerializeBothWays`, `AccountClosingDatabaseTest#tenConcurrentClosesProduceOneTransition`, `CustomerAccountDatabaseTest#tenConcurrentOpensProduceOne` | Recorded | The account-lifecycle context, joining the holds row above: `V007`'s trigger read made lock-free and the closer's `FOR UPDATE` dropped (`P3-TSK-014`); the one-live-account partial index dropped (`P3-TSK-012`) | Each lock proven load-bearing by its own deterministic interleaving, the loser observed Lock-waiting in `pg_stat_activity`: a posting in flight blocks the close, whose fresh-statement derivation then sees the money and refuses; a close in flight blocks the posting's trigger read, which on resume re-fetches `CLOSED` and refuses — the exact race a lock-free status read loses. The dropped index is caught **twice**, hermetically and by the ten-way race; ten concurrent closes produce one transition, one record, one event, nine converged |
| `INV-REV-01` | `ReversalDatabaseTest#aFullReversalLeavesTheOriginalByteIdentical`, `ReversalDatabaseTest#aReversalCannotBeReversed`, `ReversalMigrationTest#theReferenceIsAnImplication` | Recorded | `V009`'s implication `CHECK` dropped (a non-reversal could then carry a reference, a reversal could omit one); the attribution losing its reference; a replay re-entering the effect (`P3-TSK-016`) | All caught. A reversal is a **new balanced entry referencing the original** — `reverses_entry_id` is where an investigator joins the correction to what it corrects — and the original is byte-identical afterwards, on top of the standing `DB-PRIVILEGE` immutability. A reversal of a `REVERSAL` is refused at domain and schema both, because a chain would make the bound's subject ambiguous |
| `INV-REV-02` | `ReversalDatabaseTest#partialReversalsSumCorrectly`, `ReversalDatabaseTest#rawSqlCannotOverReverse`, `ReversalDatabaseTest#tenConcurrentPartialReversalsRespectTheBound`, `ReversalDatabaseTest#theLosingReversalIsObservedBlockedThenRefused`, `ReversalBoundTest#oneMinorUnitOverIsRefused` | Recorded | The trigger's refusal dropped; the advisory serializer removed; the domain check and the trigger **both** dropped (`P3-TSK-016`) | All caught — the serializer's removal by the **blocked-observation precondition** (the loser must be seen Lock-waiting on the advisory lock before the winner commits), because insert-vs-insert predicates re-evaluate against the statement snapshot and cannot see a concurrent uncommitted sibling (the `P2-TSK-015` write-skew), and the row-lock arbiters are unavailable by the phase's own privilege design. Each layer suffices alone, proven by removing both: partials 30 + 70 accepted, one more minor unit refused with nothing written, for every writer raw SQL included; ten instances reversing 400 against 1000 accept exactly two, the reversed total counted in the table |
| `INV-REV-04` | `AdjustmentEndpointDatabaseTest#anOperatorPostsAnAdjustment`, `AdjustmentEndpointDatabaseTest#aMissingReasonIs422`, `AdjustmentEndpointDatabaseTest#aSessionWithoutTheRoleIsRefused`, `AdjustmentEndpointDatabaseTest#aReplayIsTheOriginalAndAStrangersConflicts`, `AdjustmentRequestTest#theReasonBoundIsAuditRecords` | Recorded | The reason's `@NotBlank` dropped; the `@RequiresPermission(LEDGER_ADJUST)` annotation removed; the audit-action derivation dropped (an adjustment recorded as a plain posting, skipping the reason regime); the reason dropped from the idempotency fingerprint (`P3-TSK-017`) | All caught by the intended assertion. Every adjustment carries a reason bounded in three reconciled places, is authorised at its named permission's first real check site with the refusal writing **nothing**, and is audited as `ledger.AdjustmentPosted` naming the person and the justification — with the same key under a **different** justification refused as a conflict, because the reason is what makes an adjustment defensible. The four-eyes half is the `INV-AUD-04` row below (`P3-TSK-021`; the pointer here read “deliberately not claimed — see section 3” until that task built the mechanism). The named methods survived the two-step reshape with their claims intact: the POST now records the *proposal*, and the posting assertions moved to the approval inside the same tests |
| `INV-AUD-04` | `AdjustmentEndpointDatabaseTest#selfApprovalIsRefused`, `AdjustmentEndpointDatabaseTest#anOperatorPostsAnAdjustment`, `AdjustmentEndpointDatabaseTest#tenConcurrentApprovalsProduceOneEntry`, `AdjustmentProposalSchemaDatabaseTest#aRawSqlAdjustmentCannotCommitUnapproved`, `AdjustmentProposalSchemaDatabaseTest#aSelfApprovedProposalRowIsUnstorable`, `AdjustmentProposalSchemaDatabaseTest#theProposalIsFrozenForEveryWriter`, `AdjustmentProposalTest#selfApprovalIsRefusedByTheAggregate` | Recorded | The domain's self-approval check dropped from `AdjustmentProposal.requireApprovableBy` (`V010`'s CHECK then fires as our 500 where the invariant owes an actionable 409); `V010`'s `approver <> initiator` CHECK dropped; the deferred `adjustment_entry_is_approved` trigger dropped (a raw-SQL adjustment commits unapproved); the approval's `FOR UPDATE` read made a plain read (the ten-way race loses its serialisation point); the payload freeze trigger dropped; the `ledger.AdjustmentProposed` audit dropped from the propose effect; the posted entry's actor made the *initiator* instead of the approver (`P3-TSK-021`) | All caught by the intended assertion. Four-eyes is two authenticated acts — the initiator proposes and nothing posts; a **second** person approves and the entry posts in their transaction — with approver ≠ initiator held at the domain, at `V010`'s CHECK, and at the deferred journal trigger that refuses any `ADJUSTMENT` COMMIT without an approved proposal, raw SQL included. The catalogue's Verify line — *self-approval rejection tests* — finally has its subject: the refusal writes **nothing**, and a second person approves the same proposal as the positive control. The decide statement's `status = 'PROPOSED'` belt under the lock is recorded defence in depth: with the lock intact it is unreachable, and with the lock dropped it is what turns ten effects into one loud failure |
| `INV-REC-05` | `OperationalChartDatabaseTest#everyCombinationResolves`, `OperationalChartDatabaseTest#theSeamsStaySeams` | Recorded | The `Phase: 3 (accounts)` half — the designated suspense account: its seed row removed (`P3-TSK-003`) | Caught in both tiers, hermetic reconciliation and the resolve suite, so `SUSPENSE_UNMATCHED` exists in every supported currency before Phase 8 needs it — and the self-armed seam count proves **nothing posts to it in Phase 3**, live since `P3-TSK-005` created the line table. Aging, reporting and alerting are the `Phase: 8 (management)` half, owned there |
| `INV-ACC-01` | `TrialBalanceDatabaseTest#injectedImbalancesAreDetectedPerCurrency`, `TrialBalanceDatabaseTest#theSweepIsSafeUnderConcurrentPosting`, `LedgerMetricsTest#anOutOfBalanceCurrencyReadsOne`, `LedgerMetricsTest#anUnsweepableTrialBalanceReportsAbsent`, `LedgerMetricsTest#theTrialBalanceSeriesAreEager` | Recorded | Six, from `P3-TSK-019`'s sweep: the zero comparison neutralised; the currency buckets collapsed; the scale dropped from the decimal conversion; the direction sign dropped; the unknown gauge reading made zero; the eager registration removed | **All caught by the intended assertion, restores byte-identical.** Three injected shapes ride V004's deferral — equal raw sums at different scales, and the cross-currency subsidy — each flagged per currency with a committed positive control proving the detection was the imbalance; the sign-drop is caught by the positive control (a balanced journal reads out of balance); the gauge reads 0 verified balanced, 1 out, **NaN never zero** when unsweepable, eager per supported currency. No `IN_FLIGHT` verdict exists by design: one statement, one snapshot, and sweeps racing four live posters read zero every time |
| `INV-IDEM-01` | `PostingServiceDatabaseTest#tenConcurrentIdenticalKeysProduceOneEffect`, `PostingServiceDatabaseTest#aDifferentRequestOnAKnownKeyIsRefused`, `ReversalDatabaseTest#aReplayedKeyIsOneReversal` | Recorded | The financial boundary the kernel row above was built for, mutated by its owning tasks: the fingerprint made constant and the key silently made per-call — every retry a second posting (`P3-TSK-006`); a replay re-entering the reversal effect (`P3-TSK-016`); the actor and the reason each dropped from the adjustment fingerprint (`P3-TSK-017`) | All caught by the intended assertion. Ten identical keys produce one effect **counted in the table**, nine replays carrying the original entry id; a known key with a different request is a distinct conflict (`INV-IDEM-03`), never a silent second effect; the reversal's replay is one entry. ADR-0004's clause — idempotency at the financial boundary, never a cache or an HTTP filter — met by the commands that actually move money |
| `INV-HIST-03` | `AuditImmutabilityTest` | Recorded | `GRANT UPDATE, DELETE` on `platform.audit_record` to the application role; separately `GRANT UPDATE (reason)` | Table-level fails six tests, column-level two (`P0-TST-007`). The column-level widening is the one that had previously passed unnoticed |
| `INV-IDEM-01` | `IdempotencyRecordSchemaTest` | Recorded | Drop the unique constraint on (scope, idempotency_key) | 17 tests fail (`P0-TST-004`) |
| `INV-IDEM-03` | `IdempotentExecutorTest#differingFingerprintIsRejected` | Recorded | In `IdempotentExecutor.resolveExistingClaim`, remove the `fingerprint.matches` guard | **Two tests fail**, both named for the property: the conflict test and the in-progress conflict test. `P0-TSK-038` — performed during review, because this row's observed result had been *inferred* from `P0-TSK-016`'s sweep rather than recorded |
| `INV-IDEM-04` | `InboxConsumerTest` | Recorded | Drop the inbox primary key | Nine tests fail across three classes (`P0-TST-006`) |
| `INV-EVT-01` | `OutboxWriterTest` | Recorded | Move the outbox write onto its own connection, outside the business transaction | Three tests fail (`P0-TST-005`) |
| `INV-EVT-02` | `NoDirectBrokerPublicationRulesTest#ruleRejectsDirectPublication` | In-suite | A fixture publishing directly to a broker package | Rejected on every build (`P0-TSK-019`) |
| `INV-EVT-03` | `EventEnvelopeTest` | Recorded | Any of eight: drop a mandatory field, reorder two fields of the canonical form, let an emitted event inherit its parent's causation | All eight caught (`P0-TSK-018` review) |
| `INV-EVT-04` | `InboxDeliveryOrderTest` | Recorded | Delete a dedupe record, as a sweep running inside the producer's redelivery window would | The message runs twice with nothing reporting it (`P0-TST-006`) |
| `INV-AUD-01` | `AuditableActionRegistryTest` | Recorded | Plant any of three faults: an action declared but not catalogued, one catalogued but not declared, a `requiresReason` flag that disagrees | Each caught (`P0-TSK-023`). **Partial — see §3** |
| `INV-AUD-02` | `NoUnwrappedSecretRulesTest#rulesRejectTheirViolations` | In-suite | A record component and a getter-only field, each holding an unwrapped secret; and a production write to the MDC | Rejected on every build (`P0-TST-008`, which found the rule could not fail at all before it) |
| `INV-AUD-02` | `CallerCorrelationIsNotPropagatedTest#personalDataReachesNoSink` | In-suite | A **second** enforcement of the same invariant, added by `P1-TSK-002`: the wrapper rule above governs what a *type stores*, and cannot see a caller-supplied identifier the platform propagates on purpose. Mutations: adopt the inbound header again; leak it into the MDC; echo it unvalidated | All rejected. The MDC leak fails **twice** - here and at `onlyCorrelationContextWritesTheMdc` - which is the two enforcements meeting (ADR-0034) |
| `INV-AUD-02` | `CredentialNeverReachesALogTest#aRawPasswordDoesNotPrintItself` | In-suite | A **third** enforcement, added by `P1-TSK-009`, and the invariant's first *real* subject: every demonstration before it used a fixture record written for the purpose. Mutations: `Sensitive.toString` stops masking; the negative control stops leaking | Both rejected. The second matters as much as the first - a control that cannot see a leak makes every `doesNotContain` beside it vacuous |
| `INV-AUD-02` | `NoUnwrappedSecretRulesTest#rulesRejectTheirViolations` (`CompoundSecrets`) | In-suite | The `P1-TSK-009` gate **measured** the rule's vocabulary and found **six of twenty-two entries dead**: the splitter separates `apiKey` into `[api, Key]`, so the compound entries `apikey`, `privatekey`, `signingkey`, `cardnumber`, `mfacode` and `sessionid` could never match the camel-case spelling anyone actually writes. `secretKey` was caught only because `secret` is separately an entry. Mutation: each of the six as a production field | Before the fix a production `String cardNumber` **passed the build** - the PAN field ADR-0019 named to stop PCI scope widening quietly. After adjacent-pair matching, all six fail, and `idempotencyKey`, `companyName` and `spinLockName` stay clean |
| `INV-IDN-01` | `SecretsAreUnwrappedInOnePlaceTest#theUnwrapSitesAreExactlyTheOnesNamed` | In-suite | The **emission** half. `P1-TSK-007` enforced storage at `DB-CONSTRAINT`; nothing bounded where a plaintext may be unwrapped. Mutation: an `expose()` call planted in `app` | Rejected, naming the class. This is the whitelist that replaces the rejected output scrubber - see `CURRENT_STATE.md` §Known Architectural Debt |
| `INV-IDN-01` | `CredentialVerifierLogsNothingSensitiveDatabaseTest#theDiscardedUpgradeWarningIsQuiet` | Recorded | Add the identity identifier to the verifier's one production log line | Fails, naming the identifier. The line is on the *failure* path, which is the half nobody reads until something is wrong |
| `INV-IDN-01` | `RegistrationCredentialDatabaseTest#thePlaintextIsInNoColumn` | In-suite | A **fourth** enforcement, added by `P1-TSK-026`, and the first where a plaintext crosses an HTTP boundary: the sinks are the idempotency record, the audit record and the outbox row, none of which credential storage ever touched. Every column of every table in all three schemas, derived from `information_schema`. Mutation: the plaintext written into the audit change summary | Caught. The sweep also asserts which tables it reached, because one that reached none would pass while checking nothing |
| `INV-IDN-01` | `RegistrationFingerprintTest#noCredentialReachesTheFingerprint` | In-suite | The **idempotency** half, and it is asserted *structurally* because there is nothing to vary: `canonicalForm` takes no password, so the only way to break the property is to change the signature. `request_fingerprint` is a durable single-round SHA-256, so a password in the canonical form would store an offline-crackable derivation of every registration password for the life of the record. Mutation: a **second** `canonicalForm` overload taking a `RawPassword` | Caught. The overload form matters - changing the existing signature is caught by *compilation*, which proves nothing about this assertion (`P1-TSK-026`'s harness finding) |
| `INV-IDN-07` | `AuthenticationEndpointDatabaseTest#everyFailureLooksTheSame` | In-suite | The four failing causes are collected and asserted **equal to each other**, not each against a remembered expectation. Mutations: the failure event names the attempted account; the refusal is thrown so its audit record rolls back; a success is attributed to the platform rather than the proven identity | All caught (`P1-TSK-010`) |
| `INV-IDN-07` | `AuthenticationCostsTheSameDatabaseTest#everyFailingPathDoesTheWork` | In-suite | The **timing** half, and it exists because a mutation survived the response test: making a malformed password fail *without the derivation* left all four responses byte-identical and changed only how long one took. Mutation: `verifyNothing()` replaced by a bare failure | Caught only after this test was added - the response comparison could never have caught it |
| `INV-IDN-07` | `VerificationOutcomeTest` | In-suite | A response cannot be made to diverge by a single-point change: the caller is handed nothing to branch on. Mutation: add a `reason` field to `VerificationOutcome` - the first step any diverging response would need | Caught (`P1-TSK-008`'s reflective guard, re-proven by `P1-TSK-010`) |
| `INV-CON-02` | `TransferConservationDatabaseTest#valueIsConservedThroughoutSustainedBidirectionalMovement`, `TransferExecutionDatabaseTest#theTenWayDrainConservesValue` | Recorded | Three, and the first is the defect this row exists because of. **The two participants' row locks taken in no fixed order** - the source alone, which is how the execution shipped (`P4-TSK-005`) until `P4-TST-001` measured it. **The availability decision derived before the lock is granted** - still locks, still checks, still loses the race (the `P3-TST-002` shape; performed by `P4-TSK-005` against the drain and again here against the sustained composition). **The posting moving one minor unit less than the transfer row records** - both tables internally consistent, the entry balanced, the pair's total unchanged, so only a cross-table reconciliation can see it | **All caught by the intended assertion.** The unordered lock returns 495 deadlocks against 206 domain outcomes, named by the outcome tally - `INV-CON-02` requires the loser to fail with a *domain outcome*, and an infrastructure abort is not one. The pre-lock derivation drives an account to **-5.00 in round 2** of the storm, and `expected: 3 but was: 10` on the drain. The under-moving posting fails *A's journal position is exactly what its completed transfers say* (`expected: 2 but was: 0`) - the assertion it was aimed at, established rather than assumed: the same mutation **over**-moving is caught by the negative-balance assertion first, so the two are recorded separately. Intact: ten instances moving money both ways continuously, every trial-balance sweep zero per currency, every projection verdict `CLEAN`/`IN_FLIGHT`, neither account ever negative, and the pair's total unchanged **exactly** - counted from `ledger.journal_line` and independently recomputed from `transfers.transfer` |
| `INV-CON-03` | `AuthenticationLockoutDatabaseTest#theCounterIsNotBypassableByConcurrency` | In-suite | Catalogued at Phase 13 and **first enforced at Phase 1**, because ADR-0032 makes verification expensive and names lockout as part of the same design. Mutation: drop the `+ 1` so the atomic increment is lost | Caught. Ten simulated instances, own connection each, must produce exactly ten |
| `INV-IDN-07` | `AuthenticationLockoutDatabaseTest#aLockedIdentityIsNotCheaper` | In-suite | The oracle lockout creates if it is allowed to fail fast. Mutation: short-circuit the derivation for a locked identity — the CPU relief lockout appears to be for | Caught by **counting derivations**: a locked account answering in a millisecond while an unknown one takes ~46 ms is an existence oracle |
| `INV-IDN-06` | `AuthenticationLockoutDatabaseTest#anExpiredLockResetsTheCount` | In-suite | *Recovery cannot elevate an attacker* has a mirror image: a control must not permanently deny a legitimate one. Mutation: drop "a served lock ends the run" from the reset condition - the defect the gate found, where one failure after an expired lock re-locked for ever | Caught, and both halves of the condition are separately proven: removing the live-lock guard is caught too |
| `INV-CON-01` | `LoginRacingACredentialChangeDatabaseTest#theLoginStopsAtTheConditionalSupersede` | In-suite | A login racing a credential change: the hazard is that upgrade-on-use **reinstates the replaced password**. Mutations: an unconditional `supersede`; the upgrade ignoring its answer | Both caught - but only after the assertions moved from the OUTCOME to the COORDINATION. **Three** mechanisms produce the right end state here (conditional supersede, append-only trigger, partial unique index) and an outcome-only assertion let two mutations survive |
| `INV-AUD-01` | `AuthenticationFailsClosedDatabaseTest#aConnectionKilledMidFlightFailsClosed` | In-suite | Read from the other side: a failure that never committed must leave **no** record claiming it did. Mutation: write the audit record on its own connection, so it survives a transaction that failed | Caught. An audit trail saying somebody logged in when they did not is permanent under `INV-HIST-03`, and worse than no trail |
| `INV-IDN-03` | `NoProcessLocalSessionStateTest#nothingHoldsASession` | In-suite | The acceptance criterion, as a build rule. Mutation: a `Map<String, Session>` field on the production store — a cache that is not static, not a lock, and invisible to all four of ADR-0024's patterns | Caught. Transition risk **R7** was recorded precisely because those rules cannot see this shape |
| `INV-IDN-03` | `SessionLifecycleDatabaseTest#everyUnusableSessionLooksTheSame` | In-suite | Expired, revoked and never-existed must be one answer. Mutations: the lookup ignores revocation; ignores the idle bound; a touch pushes past the absolute bound; the token is stored in clear | All four caught. Each bound is asserted **alone**, because a suite testing them together passes against an implementation checking only one |
| `INV-IDN-03` | `SessionRevocationDatabaseTest#revocationIsImmediateAcrossInstances` | In-suite | The invariant's **own stated verification** — revoke on one instance, refuse on another. Mutation: **a session cache in front of the store**, which is the task's literal acceptance criterion | Caught, and caught **twice**: the behavioural test fails and `NoProcessLocalSessionStateTest` fails independently. Two controls, one defect — the point rather than a duplication |
| `INV-IDN-03` | `SessionRevocationDatabaseTest#revocationWinsAgainstAConcurrentIssue` | In-suite | *"A session must never survive a concurrent revoke"* (`PHASE_1_PLAN.md` §8). Mutations: drop the `FOR UPDATE` from bulk revocation; drop the explicit lock from issuance | The first is caught; **the second survives, correctly** — the foreign key already takes `FOR KEY SHARE`, so that lock was redundant and was removed. An outcome-only assertion caught neither, because both mechanisms leave identical rows |
| `INV-IDN-05` | `AssuranceLevelTest#theOrderingHoldsForEveryPair` | In-suite | The mechanism `INV-IDN-05` names as its own enforcement, and the completion gate found it had **no test at all**. Mutations: `atLeast` always satisfies (the boolean, restored); a level added without touching the `CHECK` constraint | Both caught. Every pair is swept rather than sampled, because the wrong pair would be the one nobody thought to write down |
| `INV-IDN-05` | `MfaCannotBeBypassedDatabaseTest` (one test per path), `MfaBypassPathsAreEnumeratedTest` | In-suite | **`P1-TST-003`.** Every enumerated path either requires the factor or cannot produce a `MULTI_FACTOR` session, and a NEW session-issuing path fails the build until it is enumerated. Mutations: an un-enumerated session path added; the re-enrolment check removed; the level check turned into equality (a boolean); elevation ending the identity's other sessions; assurance elevating every session of the identity; a refresh raising assurance | **Nine, all caught** — three added by the completion gate, which found the refused replacement's audit record unasserted and the guard checking origin *types* rather than *methods*. The suite was written after a probe found a real bypass: a `PASSWORD` session could begin a replacement enrolment with an attacker-controlled secret, refused only by a partial unique index and surfacing as a 500 |
| `INV-IDN-05` | `MfaChallengeDatabaseTest#assuranceIsRequiredAndSatisfied`, `#aReplayedCodeIsRefused`, `#aPendingFactorSatisfiesNothing` | In-suite | A second factor cannot be bypassed, and a one-time password is used once. Mutations: the replay defence removed; elevation not raising the level; the assurance requirement not enforced; a `PENDING` factor accepted; the refusal saying why; confirmation not consuming its step; guessing not throttled; a retry counted toward the lock; the lock not honoured | **Thirteen: eleven caught, two survived correctly.** Three added by the completion gate, which found the new `secretsAreWrapped` exemption resting on a test that did not exist. Four only after they found real gaps - a detail added to *every* refusal walked through an equality-between-causes, and the `PENDING` mutation was masked by `consumeStep`'s own `ACTIVE` predicate (defence in depth, verified by removing both) |
| `INV-IDN-08` | `MfaEnrolmentDatabaseTest#aStartedEnrolmentIsNotUsable`, `SecretCipherTest`, `MfaKeyTest` | In-suite | A shared secret must be encrypted under a key outside the database, and a half-enrolled factor must satisfy nothing. Mutations: `findActive` ignores status; `confirm` not conditional on `PENDING`; `discardPending` removes an ACTIVE factor; the code is never verified; the secret is stored in the clear; the RFC truncation offset is fixed; the window is unbounded; the published local default is accepted off loopback; the GCM nonce is reused; a wrong-length key is accepted; the comparison is not constant-time | **Fourteen, all resolved** — three added by the completion gate, which found the boundary was never driven over HTTP; four others only after they found real gaps: no concurrency test made the store's conditional confirm invisible, `MfaKey` and `SecretCipher` had no direct tests at all, and constant-time comparison had to be asserted **structurally** because no behavioural test can distinguish it |
| `INV-IDN-04` | `SessionOwnershipDatabaseTest#revocationIsRefusedForSomebodyElsesSession` | In-suite | Authentication is not authorization: a proven session must not carry permission over somebody else's resource. Mutations: drop `AND identity_id = ?` from the ownership predicate; drop it from the listing; revert `SessionRevocation` to the unowned revoke; report 403 rather than 404; accept a bare `Authorization` header; leave the security scope open; enter as the platform rather than the person; drop the device `CHECK`; never extend the idle bound | **Thirteen, all caught** — three added by the completion gate, and three only after they found real gaps: the raw-header shape used a *revoked* session (refused either way), no test asserted the scope was closed, and the fail-closed test was too loose to distinguish a swallowed storage failure reported as `401` |
| `INV-IDN-05` | `AuthenticationIssuesASessionDatabaseTest#aLoginEstablishesPasswordAssurance`, `#aLoginSessionDoesNotSatisfyAMultiFactorRequirement` | In-suite | **The second way a session comes into existence** (`P1-TSK-027`). A login must establish `PASSWORD` and nothing more, and the level is not a parameter - a caller able to ask for `MULTI_FACTOR` would have found the bypass this invariant exists to prevent. Mutations: the session issued at `MULTI_FACTOR`; the new path not enumerated in `MfaBypassPathsAreEnumeratedTest` | Both caught. The second is the one that matters for the *next* path: the guard predicted this task by name and failed until it came and wrote down why the path is not a bypass |
| `INV-IDN-05` | `SessionRotationDatabaseTest#theOldIdentifierIsRefused` | In-suite | Session fixation: elevating in place lets an identifier stolen *before* the elevation become elevated. Mutations: reset the absolute bound on rotation; ignore the conditional revoke's row count; log the rotation as a revocation; make the predecessor's token reachable; **skip replacement when the assurance level is unchanged** | All caught — the last added by the completion gate, which found the same-level rotation (the credential change) untested. A sixth — deriving the new token from the old — **survived correctly**: it derived from the *hash*, which an attacker never holds, and the dangerous version is structurally unreachable |
| `INV-IDN-04` | `DenyByDefaultDatabaseTest#anUndeclaredEndpointIsRefused`, `EveryEndpointDeclaresARuleTest#everyHandlerDeclaresSomething` | In-suite | *A rule's absence is never a grant* (ADR-0031). Mutations: deny-by-default removed; the permission check made a no-op; a revoked role still grants; a denial leaves no trace; a production endpoint loses its declaration; the role grants nothing; a permission-only handler is not recognised as declared | **Thirteen, all caught** - six added by the completion gate, which found a handler declaring **both** `@Unauthenticated` and `@RequiresPermission` answered **200 with no session**: the interceptor returns early and the build guard is satisfied by *any one* declaration, so an endpoint that **reads as protected** was public. Enforced twice and neither replaces the other - a static sweep cannot see a handler registered at run time, and a runtime check cannot fail a build. **Recorded limit:** with one role holding both permissions the mapping cannot be meaningfully mutated - the first attempt (return *all* permissions) was a **no-op** and was re-aimed at the role granting *nothing* |
| `INV-CON-01` | `RoleAssignmentConcurrencyDatabaseTest`, `RoleAssignmentMigrationTest` | In-suite | Role assignment under N instances, and the enum-to-constraint reconciliation `V010` claimed and did not have. Mutations: the conditional grant ignores its row count; revocation is unconditional; a role is added without touching the `CHECK`; the granted set becomes mutable | **All caught.** Two live rows for one identity and role would make revocation **partial** - `revoke`'s row count is the outcome, so it would report success having revoked one of them and the identity would keep the role |
| `INV-IDN-04` | `OwnershipIsScopedTest`, `SessionOwnershipDatabaseTest#revocationIsRefusedForSomebodyElsesSession` | In-suite | ADR-0031's ownership half: *may **this** actor act on **this** resource?* Mutations: an owner-scoped statement loses `identity_id = ?`; a new unclassified resource-scoped operation; an `AUTHORITATIVE_ID` entry naming a read that does not exist; a named negative test that does not exist; an owner-scoped operation with no negative test; `party` gains a resource-scoped operation; the named provenance read stops being owner-constrained | **Ten, all caught** - two added by the completion gate, which found the rule aimed at the wrong half: the shape this repository has actually shipped is a method that takes an owner and **never uses it**, and a `findLiveFor` losing its scope is a **bulk** disclosure the resource-identifier rule structurally cannot see. The predicate removal and the listing scope are each caught **twice**, by the build rule and by the behavioural suite. **The first version of the build rule SURVIVED its own mutation**: it searched the whole method body, which contains the comment *“identity_id = ? IS the ownership check”* - a `contains` over source text matches prose. It reads string literals only now |
| `INV-AUD-01` | `AuditCompletenessTest`, `SystemActorCallSitesAreEnumeratedTest`, `AuditNamesTheActorDatabaseTest` | In-suite | Every privileged action produces a record naming the **real actor**. Mutations: an action silently stops being emitted; a new action nobody emits or declares; a stale `NOT_YET_EMITTED` entry; a new un-enumerated `enterSystem()`; the login success attributed to the platform; a privileged action naming the platform rather than the person; a blank target; a correlation identifier that joins to nothing | **Ten, all caught** - two added by the completion gate, which found a claim the test's own display name made and nothing asserted, and a coverage guard deviating from its siblings for the second task running. This closes the limit `P0-TSK-023` recorded against itself - *the registry cannot detect a privileged action that writes no record at all* - which is the failure that matters, because a registry agreeing with a catalogue while nothing emits half of it looks complete from both sides. **Which assertion is load-bearing was established by probing**: the blank-target mutation is caught by `AuditRecord.bounded` refusing construction, not by the field assertions, so those are recorded as defence in depth rather than as the control |
| `INV-IDN-06` | `RecoveryAbuseDatabaseTest` (nine tests, one per route) | In-suite | *Recovery cannot elevate an attacker.* Mutations: an unverified channel can recover; a token is replayable; a token survives a credential change; recovery leaves sessions alive; cooling-off removed; an earlier token survives a later initiation; a channel is readable by another identity; a channel can be re-verified; the initiation record is unfindable by account; a suspended identity can recover; the verification token is not cleared | **Eleven: ten caught, one survived correctly.** Two survivors each found a real gap first - `aCancelledTokenIsRefused` was passing for the WRONG REASON (it looked up the latest request, which after the customer's initiation is theirs), and nothing had ever suspended an identity. The survivor is re-verification, unreachable because the token is cleared on success - proven by the mutation that removes the clearing |
| `INV-IDN-02` | `CredentialSchemaDatabaseTest#theParametersAreMandatory`, `CredentialSchemaDatabaseTest#costFactorsMustBePositive`, `CredentialMigrationTest#algorithmsAgree` | In-suite | Make one of the four derivation columns nullable; separately, drop the `> 0` check on a cost factor | Both fail (`P1-TSK-007`). **Added by `P1-TSK-024`, which found this invariant had no row at all** - the one the task was named for, and the register had been extended six times during the phase without it |
| `INV-AUD-03` | `DenyByDefaultDatabaseTest#negativeAuthorizationForPrivilegedEndpoints`, `DenyByDefaultDatabaseTest#anUndeclaredEndpointIsRefused` | In-suite | Remove the permission check from the interceptor; separately, remove deny-by-default | Both caught (`P1-TSK-020`). **Also added by `P1-TSK-024`, and it is the row a narrower extension would have missed**: it is `Phase: 1 onward` and is not in the `INV-IDN` group, so extending the guard to `INV-IDN-*` - which is what this task's own text asked for - would have left it out |
| `INV-KYC-01` | `VerificationAdapterTest#unknownStateIsIndeterminate`, `VerificationRunDatabaseTest#aHitBlocks`, `VerificationRunDatabaseTest#evidenceIsAppendOnly` | Recorded | A provider verdict made the decision: the port's unknown-verdict default mapped to `CLEAR` instead of `INDETERMINATE`; a `HIT` allowed past the assessment; the evidence write dropped, so the verdict would be the only record of what the provider said | Each caught by the intended assertion (`P2-TSK-009`). The adapter matrix holds the port **total** - every misbehaviour is a result, never success - a `HIT` never terminates the case (`IN_REVIEW` has no edge to a terminal), and the bytes received are retained verbatim and append-only. No decision exists until the platform's own act: a reviewed case with every verdict in stays undecided (`DecisionDatabaseTest#anUnreviewedCaseIsNotReady`) |
| `INV-KYC-02` | `DecisionDatabaseTest#theDecisionIsImmutableAtThePrivilegeLevel`, `DecisionDatabaseTest#theAutomaticDecisionReplays` | Recorded | `GRANT UPDATE` on the decision tables in `V007`; separately the reviewer's audit written as the platform, and the evidence references dropped | All caught (`P2-TSK-013`), the grant mutation against a **from-scratch** database. There is no `UPDATE` grant at all, so the privilege IS the immutability - the `audit_record` model - with `UPDATE`/`DELETE` proven denied on every column of both tables. The replay test re-derives the stored outcome from the check rows the record references and the pinned policy version - `INV-CRD-01`'s regime, three phases early |
| `INV-KYC-03` | `ProviderCallbackDatabaseTest#aDuplicatedDeliveryHasOneEffect`, `ProviderCallbackDatabaseTest#tenConcurrentDeliveriesProduceOneEffect`, `DecisionDatabaseTest#tenConcurrentDecisionsProduceOne` | Recorded | The inbox dedupe bypassed, the effect run directly; the conditional completion made unconditional, so a late callback reopens a terminal check; the decision's own arbiter (`READY_FOR_DECISION` &rarr; terminal) ignored | Each caught (`P2-TSK-011`, `P2-TSK-013`). Two dedupe layers blind in different directions - the inbox absorbs **identical** deliveries by `deliveryId`, the conditional transition absorbs **distinct** re-sends - and N concurrent deciders produce one decision with N-1 losers told the truth: a triple delivery and a ten-way race each land one completion, one evidence row, one audit record |
| `INV-KYC-04` | `ScreeningRunDatabaseTest#aHitBecomesWorkForAPerson`, `KycDecisionTest#theAutomaticPolicyRefusesANonClearCheck`, `ReviewDatabaseTest#bothEndpointsRefuseASessionWithNoRole` | Recorded | A `HIT` allowed past the assessment; the routing to `IN_REVIEW` removed, so a hit case never becomes work for a person; the automatic policy's non-`CLEAR` predicate removed - an automatic approval of a case that owed a person a judgement | Each caught (`P2-TSK-009`, `P2-TSK-010`, `P2-TSK-013`). Structurally the case machine has no edge from `IN_REVIEW` to a terminal, `ChecksAssessment` decides `HIT` first so not even a later `CLEAR` of the type un-blocks, and the automatic refusal is proven **at the domain** because the `BLOCKED`-first ordering makes it unreachable in production. Resolution requires `KYC_REVIEW` (negative authorization test), a reason, and is audited naming the reviewer |
| `INV-KYC-05` | `CustomerProjectionDatabaseTest#theDecisionAndTheProjectionCannotDrift`, `CustomerProjectionDatabaseTest#aClosedCustomerRefusesTheDecisionWhole`, `PartyModuleIsolationTest#seesNoSiblingAndNoCompositionRoot` | Recorded | The projection dropped from either door; the `DecisionOutcome` &rarr; `CustomerStatus` mapping inverted; the lost-move failure swallowed, so a decision is recorded beside an unmoved customer - the silent drift this invariant forbids | Each caught (`P2-TSK-014`). The reconciliation sweep holds both directions - the sharp one being that **no customer under verification leaves `PENDING` without a decision authorizing it** - a lost projection fails the whole transaction loudly with nothing written, and the module boundary keeps `party` unable to see `kyc`, so nothing outside the orchestration can compute the status |
| `INV-CNS-01` | `ConsentGateDatabaseTest#theCausesOfRefusalAreOneAnswer`, `ConsentEndpointDatabaseTest#absenceAndWithdrawalAreOneAnswer`, `ConsentHistoryDatabaseTest#theDerivationFollowsTheHistory`, `CustomerOpenedOpensCaseTest#aRefusedOpeningWritesNothing` | Recorded | The `GRANT` predicate dropped from the derivation, so a **withdrawal** derives a basis; the gate made to permit everything; the consumer opening without asking the gate at all; separately the query's per-purpose derivation inverted | Each caught by the intended assertion (`P2-TSK-017`, `P2-TSK-018`, `P2-TSK-019`). Absence and withdrawal are asserted as an **equality between the causes** - at the store, and byte-for-byte over HTTP - so neither can be told from the other rather than each merely matching what its author wrote down. The consumer mutation is caught hermetically by the store never being asked: an open attempted and converged away would still have been an attempt to process without a basis |
| `INV-CNS-02` | `ConsentHistoryDatabaseTest#theHistoryIsImmutableToTheApplication`, `ConsentHistoryDatabaseTest#textsArriveOnlyByMigration`, `ConsentMigrationTest#recordGrantsAreThePrivilegeModel`, `ConsentMigrationTest#textsAreUnwritableByTheApplication` | Recorded | `GRANT ... UPDATE` added on `consent_record` in `V002`; separately `GRANT ... INSERT` added on `consent_text` | Both caught against a **from-scratch** database (`P2-TSK-017`). There is no `UPDATE` grant at all, so the privilege IS the immutability (the `audit_record` model, no freeze trigger), proven per column from `information_schema` - and the sweep probes `seq` with `= DEFAULT` rather than `= seq`, because `GENERATED ALWAYS` refuses the latter before the privilege check runs, which would have rested the assertion on the wrong refusal |
| `INV-CNS-03` | `ConsentWithdrawalBlocksTheCapabilityDatabaseTest#aWithdrawalOnOneInstanceBlocksCaseOpeningOnAnother`, `ConsentGateDatabaseTest#withdrawalOnOneInstanceRefusesOnAnother`, `NoProcessLocalConsentStateTest#nothingHoldsConsentState` | Recorded | The gate's authoritative read replaced by a cached value, twice: memoised per (party, purpose) on the **gate**, and - one layer down, where the field detector structurally cannot see it - memoised inside `JdbcConsentStore.hasCurrentBasis` | Both caught, at the gate **and at the capability** (`P2-TST-002`) - the milestone bullet names the capability, so the real consumer is driven with the real stores, gate and writers rather than the property being assembled from two green tests. The race holds instance B's transaction open and straddles the **commit**: while the withdrawal is uncommitted A still permits (an instance refusing there would be reading dirty), and on A's very next decision after the commit it refuses. **The detector SURVIVED the store-level cache** - a `Map<String, Boolean>` names no consent type - so the behavioural test is the load-bearing control for that shape and the detector is the second, blind in a different direction |
| `INV-CNS-04` | `ConsentHistoryDatabaseTest#theVersionPinIsUnforgeable`, `ConsentHistoryDatabaseTest#reconsentIsAPropertyOfTheVersion`, `ConsentMigrationTest#theVersionPinIsComposite`, `ConsentEndpointDatabaseTest#aStaleGrantIsRefusedWhenReconsentIsDemanded`, `ConsentGateDatabaseTest#aStaleBasisRefuses` | Recorded | `text_version` made nullable; the composite FK `(purpose, text_version)` dropped; the re-consent clause dropped from the derivation; the boundary's stale-version refusal dropped | Each caught (`P2-TSK-017`, `P2-TSK-018`). The two schema mutations are **independently** load-bearing: SQL's composite-FK semantics skip the check when any column is NULL, so dropping the NOT NULL lets an unpinned record through the FK that is still present - which is why the schema carries both. Whether a version lapses earlier grants is read from the version's own `requires_reconsent`, so the gate's next decision refuses with no restart and no sweep in between |
| `INV-KYC-06` | `DocumentUploadDatabaseTest#theReadPathDecryptsVerifiesAndAudits`, `DocumentUploadDatabaseTest#anUploadStoresEncryptedChecksummedEvidence`, `DocumentCipherTest#tamperIsRefused`, `DocumentCipherTest#wrongKeyIsRefused` | Recorded | The audit write dropped from `DocumentAccess.read`, so content is delivered with no trail of who looked; and the content persisted in the clear, so the ciphertext column holds the plaintext | Both caught, and **performed by this review** rather than inferred (`P2-DOC-001`; the `P0-TSK-038` review's finding that one row's observed column had been reasoned to). The unaudited read is caught by exactly the intended assertion, naming *“the read path decrypts, verifies the checksum, and audits who looked”*; the plaintext storage is caught by the `information_schema` column sweep among four others, because content stored in the clear also fails to decrypt on the way back. The tamper and wrong-key halves are **In-suite** fixtures that run on every build — GCM answers *is this the ciphertext this key wrote*, and the separate checksum answers *are these the bytes received*, which is why a ciphertext the same key genuinely wrote is still refused |
| `INV-HIST-02` | `VerificationRunDatabaseTest#aCleanRunReachesReadyForDecision`, `VerificationRunDatabaseTest#evidenceIsAppendOnly`, `ProviderCallbackDatabaseTest#aLateCallbackIsEvidenceOnly` | Recorded | The evidence append dropped from the run path, so a provider's answer is normalised into an outcome and the bytes that produced it are gone | Caught by the intended assertion — *“a clean run takes the case to APPROVED with retained evidence”* — **performed by this review** (`P2-DOC-001`), and by the owning tasks' own sweeps before it (`P2-TSK-009`, `P2-TSK-011`). The append-only half is at `DB-PRIVILEGE`: the evidence table grants `SELECT, INSERT` and nothing else, so our interpretation can never overwrite the original. **This row is the review's own finding**: `INV-HIST-02` is `Phase: 2 (screening), 5 (providers), 8 (files)`, so it belongs to Phase 2 while sitting outside both of the phase's named groups — the plan, the transition and the review's first draft all counted *ten* Phase 2 invariants and there are **eleven**. `P1-TSK-024`'s finding, exactly: `INV-AUD-03` was missed the same way, for the same reason |

## 3. What the register does not claim

**`INV-AUD-01` is demonstrated only in half.** The registry proves that a *recorded* action is one
the catalogue knows about. It cannot detect a privileged action that writes **no audit record at
all**, and `P0-TSK-023` recorded that limit rather than glossing it: a registry that looked
complete while the calls were missing would be worse than none, because it would be believed. The
missing half needs the Phase 15 audit-completeness verification.

**`INV-AUD-04`'s row could not be written until `P3-TSK-021`, and now exists** — kept here
because the episode is the register's doctrine working end to end. `P3-TST-003` found the
mechanism deliberately unbuilt (no threshold, no second approver, four-eyes recorded as
ADR-0010's debt by `P3-TSK-017`), refused to write a row naming the adjustment suite's
authorization tests — a false row is worse than a missing one, because it is believed — and
recorded instead that the battery would fail naming `INV-AUD-04` at the status flip, since
`MutationDemonstrationTest` derives its demanded set from the catalogue. `P3-TSK-021` built
the mechanism (two authenticated acts: propose, then a second person's approval posting the
entry; approver ≠ initiator at `DOMAIN` and `DB-CONSTRAINT`, the invariant's own Enforce
clause) and landed the row in §2 with the self-approval rejection tests the catalogue's
Verify line names — before `P3-DOC-001`, exactly as scheduled.

**Two invariants are enforced by the type system, and a mutation is a compile error.**
`INV-MON-02`'s domain half — there is no no-currency constructor — and `INV-EVT-03`'s
all-fields-mandatory construction cannot be broken at run time, so the register records the
half that *is* mutable (the schema constraint, the field set). `P0-TSK-012` handled the same
situation by invoking `javac` on the substitution; that technique is available when a compile-time
property is worth demonstrating directly.

**A cache has two directions, and one test cannot see both.** `INV-CNS-03`'s demonstration catches a gate that remembers a **permission** - that is the direction the invariant forbids, because it keeps a withdrawn basis alive. A gate that remembered only **refusals** was probed and **survived** it, correctly: withdrawal still takes effect immediately, and what goes stale is a refusal, so nothing `INV-CNS-03` says is violated. The symmetric defect is real all the same - a person who has just granted stays blocked - and it is caught at the other door, by `RegistrationOpensCaseKafkaTest#theGateHoldsTheEagerDoorAndAConsentedOpenLands`, whose second half opens a case for a party that was refused moments earlier. Recorded here because neither test covers the property alone, and a reader of either one would reasonably assume it did (`P2-TST-002`).

**A general test is not automatically the protecting one.** `INV-MON-05`'s mutation is *not* caught
by `roundTripsEveryCurrencyScale`, which writes amounts whose scale already matches the currency's
current minor units — so re-deriving gives the same answer and the test passes. Only
`storedScaleSurvivesIndependentlyOfCurrentCurrencyData` fails. Naming the *method* rather than the
class is therefore part of the convention wherever one method carries the property.

---

## 4. The register — `P{n}-TST-*` items

The task's own acceptance criterion: the convention is applied to every `P0-TST-*` item.

| Item | Its criterion's mutation | Observed |
|---|---|---|
| `P0-TST-001` | Break rounding, currency checking or overflow handling | All three named breaks fail. A later sweep found three surviving mutants and closed two real gaps — a reversed `compareTo`, and `equals` ignoring scale |
| `P0-TST-002` | Change the allocator to naive division | The sweep fails. Both sweeps also assert they encountered indivisible remainders, so neither can pass by allocating only divisible amounts |
| `P0-TST-003` | Remove propagation from a sink | The test fails, with a negative control proving an unwrapped handoff loses the identifier |
| `P0-TST-004` | Drop the unique constraint | 17 tests fail |
| `P0-TST-005` | Move the outbox write outside the business transaction | Three tests fail |
| `P0-TST-006` | Drop the inbox primary key | Nine tests fail across three classes |
| `P0-TST-007` | Widen the privilege grant | Six tests at table level, two at column level |
| `P0-TST-008` | Add a sensitive field without redaction | The rule rejects it — after `P0-TST-008` fixed the rule, which could not fail at all |
| `P0-TST-009` | Revert the `V004` fix so the lease is judged by the client's clock | The corrected skew test fails. Before the correction it stayed green, which is the defect that task found |

---
| `P1-TST-001` | Plant an unwrapped `String lastPassword` in real production code; separately, narrow the unwrap whitelist | The build fails twice for the field - once for the component, once for the accessor - and the whitelist rejects a new `expose()` anywhere. `P1-TSK-009` also found the rule's vocabulary was **one third structurally unreachable**, and a `cardNumber` field passed cleanly before the fix |
| `P1-TST-002` | Make `supersede` unconditional; separately, make the failure-counter predicate unsatisfiable | Caught only after the test was rewritten to assert the **coordination** rather than the outcome - three mechanisms produce the same end state, and an outcome-only assertion cannot name which one did (`P1-TSK-012`) |
| `P1-TST-003` | Add an un-enumerated session-issuing path; separately, turn `atLeast` into equality | The enumeration guard fails on the first, and `assuranceIsALevelAndNotABoolean` on the second. The suite was written after a probe found a **real bypass**: a `PASSWORD` session could begin a replacement enrolment with an attacker-controlled secret (`P1-TSK-019`) |
| `P2-TST-001` | Reopen a terminal case state or drop the aggregate's transition check; map a provider verdict onto success; deliver one callback three times and ten ways concurrently | The cross-product sweep (`KycCaseLifecycleTest#everyTransitionIsEnforced`) fails on both lifecycle mutations (`P2-TSK-005`); the adapter matrix fails the mapping (`P2-TSK-009`); the duplicate tests fail when either dedupe layer is bypassed, and one decision survives the ten-way race (`P2-TSK-011`, `P2-TSK-013`). The item's own deliverable is the five `INV-KYC` rows in §2, which the register guard begins demanding the day Phase 2's status flips `COMPLETE` |
| `P2-TST-002` | Replace the gate's authoritative read with a cached value | `ConsentGateDatabaseTest#withdrawalOnOneInstanceRefusesOnAnother` fails, for a gate-level cache and for one hidden a layer down in the store. **Performing it changed the test**: written with one `ConsentGate` shared by both simulated instances it caught the mutation by accident (A memoises before B withdraws), and `SimulatedInstance`'s own rule - never share the thing whose sharing hides the defect - demanded a gate per instance. Moving to that revealed the test had been resting on autocommit, and it now straddles the **commit** instead: uncommitted, A still permits; committed, A's next decision refuses. Two further probes bound the claim - the field detector survives a store-level cache, and a refusal-only cache survives this test and is caught by the kafka door. Reading the item's own words also found the demonstration aimed one level too low: it proved the **gate** flipped and left the **capability** to a composition argument, so `ConsentWithdrawalBlocksTheCapabilityDatabaseTest` drives the real consumer and asserts no case, no audit record and no announcement - with a positive control, so a capability that opened nothing ever could not pass it |
| `P3-TST-001` | Update the projection outside the posting transaction | Five tests fail across `BalanceProjectionDatabaseTest` and `SustainedConcurrentPostingDatabaseTest`, the two intended assertions among them - the rolled-back posting deterministically, and the sustained storm's drift-free-throughout sweep. The item's own deliverable is the `INV-BAL-02` row in section 2 and the sustained composition itself: the storm is ended by the VERIFIER (sweeps and committed-entry floors both met before it stops the posters), so the overlap is a property of the loop's exit condition rather than of scheduling luck, and the final row is held to the posters' own committed tally, tracked outside the kernel. The rebuild half records the operator procedure that is safe under live posting - lock-then-look, because a blocked `UPDATE` re-evaluates recomputing subqueries against its ORIGINAL snapshot (`P2-TSK-015`'s finding) and would lose exactly the posting it blocked on |
| `P3-TST-002` | Remove the account row's `FOR UPDATE`; move the availability check's inputs outside the lock; read the holds from the projection instead of the hold rows | The removed lock fails the deterministic interleaving's blocked-observation precondition; the moved check keeps that precondition green and fails the outcome half plus the ten-way count - **performed by this item**, because `P3-TSK-015`'s sweep had performed the check *dropped* and moved-outside is the different defect that still locks, still checks and still loses the race; the projection read fails the corrupted-`holds_minor` test. The item's own deliverable is the `INV-CON-01` and `INV-BAL-04` rows in section 2; `INV-BAL-05`'s row is **deferred in writing to the exit review** (the `P2-TST-001` handling of `INV-KYC-06`), its demonstration already performed and recorded inside the `INV-BAL-04` row |
| `P4-TST-001` | Take the two participants' row locks in no fixed order (the source alone); derive availability before the lock grant; post one minor unit less than the transfer records | **The demonstration found a real defect, which is what a composition item is for.** Ten instances moving money both ways between one pair produced **783 deadlocks against 203 domain outcomes** - the `journal_line` foreign key takes `FOR KEY SHARE` on the destination (`P3-TSK-014`'s mechanism) while the execution held `FOR UPDATE` on the source, so A->B and B->A are a cycle. The one-directional drain could not reach it, because a cycle needs two directions, and the method's own javadoc had said for three tasks that *the destination is deliberately never locked* - true of the explicit lock, false about what happens. Remedied with the idiom the sibling store method already names (`lockOwnedForUpdate` orders by id *so two multi-account closers cannot deadlock*), and the storm went from 206s and 79% aborts to 4.5s and none. **A surviving mutation then corrected the test**: the pre-lock availability derivation survived the first draft, because amounts of 1.00-3.00 against 10.00 never bring an account near zero and a stale read only differs at the boundary - with 7.00 and 9.00 in the rotation it is caught mid-storm at -5.00. The item's own deliverable is the `INV-CON-02` row in section 2; the other four `Phase: 4` rows are `P4-TST-002`'s, read from the catalogue |
| `P3-TST-003` | The audit-not-invent sweep over every `Phase: 3` invariant, with the set read from the **catalogue** and not the plan | **The set is nineteen, and the plan's own section 6 table lists seventeen** — `INV-REC-05` and `INV-AUD-04` sit outside it, the exact drift the item's scope predicted (`INV-HIST-02`'s class; the plan's closing paragraph already rules the catalogue wins, so the finding is recorded rather than the table edited). Fourteen rows, two extension rows and a financial-boundary `INV-IDEM-01` row landed — seventeen, counted — every named mutation one its owning task **performed** — the `P2-TST-001` posture, record rather than re-perform — and every named class and method held to the code by this register's guard from the moment the rows landed. `INV-BAL-05`'s row lands here, earlier than `P3-TST-002`'s recorded deferral, because this item's scope demands every row and what the deferral postponed was the record, not the work. **The sixteenth row cannot be written**: `INV-AUD-04`'s mechanism is deliberately unbuilt (section 3), so the item's deliverable there is the finding, the section-3 entry, and `P3-TSK-021` created to land the mechanism and its row before `P3-DOC-001`. The F1–F8 assessment itself is `docs/project/reviews/PHASE_3_FINANCIAL_SUPPLEMENT.md`, each criterion held to named tests the register also names |

## 5. Applying this to a new test

1. Write the test.
2. Break the property it protects — in production code, in the schema, or in a fixture.
3. Run the tests. **Read the names of what failed**, not just the count: a mutation caught by an
   unrelated assertion is not a demonstration, and this repository has twice reported one as caught
   when a different test had objected.
4. Revert, and confirm the revert landed. `git checkout --` is a no-op on an untracked file and has
   three times destroyed uncommitted work on a tracked one; copy-based backup is the reliable form.
5. Record the mutation, the command and the result — here for an `INV-*`, in the change log
   otherwise.

**Isolate the mutation to the assertion under test.** `P0-TSK-031` changed two occurrences at once
and the failure came from a different guard entirely, which reported the mutation as caught when
the assertion under test had let it through.

---

## 6. What is enforced

`MutationDemonstrationTest`, on every build:

1. Every invariant `FINANCIAL_INVARIANTS.md` marks as belonging to **a phase this project has
   reached** has a register row in §2. The current phase is read from `CURRENT_STATE.md`
   (`P1-TSK-024`), so Phase 2 needs no change here.
2. Every `P{n}-TST-*` item in `BACKLOG.md` has a register row in §4 — found wherever a **bold
   heading** declares it, because Phase 0 gives those items their own headings and Phase 1 names them
   inside task headings, and anchoring to either shape passes vacuously for the other.
3. Every invariant §2 names **exists in the catalogue** — the other direction, so a row that has
   quietly stopped applying to anything is not indistinguishable from one that still does. Added
   during review, which found a planted `INV-ZZZ-99` row passing cleanly.
4. Every test class named in either register **exists**.
5. Every method named in **any** row **exists on that class** — so a claim of continuous
   proof cannot point at a method that was renamed or deleted. This caught the `INV-IDEM-03` row
   written during review, which named a method that does not exist.
6. **Every row names at least one test the parser can read** (`P1-TSK-024`). Checks 4 and 5 are
   satisfied vacuously by a row that parses to nothing, and that is not hypothetical: the grammar
   admitted exactly one backticked reference and **nine rows did not parse**, eight of them written
   during Phase 1, so the tests they named were never checked. The check is **per row** rather than
   per invariant, because an invariant may have two rows and merging their references lets an
   unreadable one hide behind a readable sibling — established by a mutation that survived the first
   version of this very check.
7. **Every §2 line that looks like a row parses as one** (`P1-TSK-024`, completion gate). Check 6
   sees a row that parses and names nothing; it cannot see a row that fails the pattern **entirely**,
   because such a row is not in the map at all. A form column reading `Insuite` rather than
   `In-suite` left the build green, and the invariant stayed covered only by its sibling rows.
6. Both forms are present and labelled, so the form column cannot quietly stop carrying
   information.
7. The registers are actually parsed, so a reformatted table fails loudly rather than silently
   matching nothing, and the in-suite rows are asserted to have actually resolved.

**Not enforced:** that a `Recorded` demonstration still reproduces. Re-running one means mutating
production code or the schema, which a build must not do to itself. That is the residual risk the
form column exists to make visible, and the reason to prefer in-suite proofs.
